# Quotes API 🧠

A simple Python Flask API that returns a random inspirational quote.

## 🚀 How it works
The app defines a single endpoint:

It returns a random quote in JSON format, for example:
```json
{"quote": "Stay hungry, stay foolish."}
