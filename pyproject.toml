[project]
name = "quotes-api"
version = "0.1.0"
description = "Simple Flask API that returns a random quote"
requires-python = ">=3.10"
dependencies = ["flask"]

[tool.pytest.ini_options]
pythonpath = ["src"]